; =============================================================================
; extensions.conf — Dialplan antigo (EXEMPLO - versao sem AGI)
;
; Este arquivo e apenas referencia historica.
; O dialplan atual esta em web/deploy/extensions.conf.new
; =============================================================================

; Este era o dialplan original antes da implementacao do portal web.
; Cada ramal tinha horario controlado diretamente no dialplan.
; Agora o controle e feito via AGI + banco de dados (SQLite).
;
; Veja web/deploy/extensions.conf.new para a versao atual.
