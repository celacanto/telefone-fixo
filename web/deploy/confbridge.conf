; =============================================================================
; confbridge.conf — Perfis para chamada em grupo
; Adicionar ao /etc/asterisk/confbridge.conf no servidor
; =============================================================================

[telefone_bridge]
type=bridge
max_members=5
sound_join=beep
sound_leave=beep

[telefone_user]
type=user
