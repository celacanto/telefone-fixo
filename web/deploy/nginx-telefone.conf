server {
    listen 80;
    server_name TROCAR_POR_DOMINIO.duckdns.org;

    # Redirecionar HTTP para HTTPS (apos Let's Encrypt configurado)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Descomentar apos certbot configurar HTTPS:
# server {
#     listen 443 ssl;
#     server_name TROCAR_POR_DOMINIO.duckdns.org;
#
#     ssl_certificate /etc/letsencrypt/live/TROCAR_POR_DOMINIO.duckdns.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/TROCAR_POR_DOMINIO.duckdns.org/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
